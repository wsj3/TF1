module.exports = {
  plugins: {
    // Empty object for tailwindcss to satisfy the import but not do any processing
    'tailwindcss': {},
    // Keep autoprefixer for browser compatibility
    'autoprefixer': {},
  },
} 